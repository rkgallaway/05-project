'use strict';

module.exports = (bmp) => {
  if(! colorArray.length ){
    throw 'Ivalid format';
  }
  
  for(let i = 0; i < bmp.colorArray.length; i+=4){
    if()

  }
  console.log('made a greyscaled verion');
};