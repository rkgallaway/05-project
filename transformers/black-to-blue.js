'use strict';

module.exports = (bpm) => {
  if ( ! bpm.colorArray.length ){
    throw 'Invalid File Format';
  }
  for(let i = 0; i < bpm.colorArray.length; i++) {
    if( bpm.colorArray[i] ===  )
  }
}

